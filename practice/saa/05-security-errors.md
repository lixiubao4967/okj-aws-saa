# P4 安全 + 监控 — 错题记录

## 成绩趋势

| 日期 | 模块 | 得分 |
|------|------|------|
| 2026-09-13 | KMS + 密钥凭据管理 | **9/10（90%）** ✅ |
| 2026-09-13 | 监控审计四件套 | **9/10（90%）** ✅ |
| 2026-09-13 | 防护五件套 | **10/10（100%）** 🎯 |
| — | **P4 合计** | **28/30（93%）** |

答对：CMK 判据、信封加密、KMS 双重授权、Secrets Manager 轮换、Parameter Store 成本、
　　　IAM Role（识破 Parameter Store 存 access key 的诱饵）、External ID、CloudHSM、反向题 Q10
答错：加密 EBS 快照跨 Region 复制

---

## 错题 14：加密 EBS 快照跨 Region 复制

**场景：** 加密的 EBS 快照要复制到另一个 Region 做灾备，关于 KMS 密钥哪项正确？

**我的答案：** C) 快照不支持跨 Region 复制　**正确答案：** D) 复制时需指定目标 Region 的 KMS 密钥

### 错因 —— 选了一个「功能不存在」的选项

**加密的 EBS 快照完全支持跨 Region 复制**，这正是最标准的灾备做法。

### 知识点：复制过程中密钥会换掉

```
源 Region (ap-northeast-1)          目标 Region (us-west-2)
  快照（用 KMS-Key-Tokyo 加密）
         │
         │  CopySnapshot 时指定
         │  KmsKeyId = KMS-Key-Oregon
         ▼
                                    快照（用 KMS-Key-Oregon 加密）
```

**为什么必须换密钥**：KMS 密钥是 **Region 级资源**，东京的密钥在俄勒冈不存在，
目标 Region 无法用它解密。AWS 在复制时**解密 → 用目标 Region 密钥重新加密**
（内部完成，你只需指定目标密钥）。

> 🔑 串记 **Multi-Region Keys**：嫌每次指定麻烦，就用它 —— 同一密钥 ID 在多 Region
> 各有副本，复制时不用重新指定。

### 🚨 新增解题规则：警惕「XX 不支持 YY」类选项

这次的错误类型和之前不同：
- 之前错题（Athena 联邦查询、DynamoDB join、Firehose 重放）= **把某服务能力记错**
- 这次 = **否定了一个实际存在且常用的能力**

```
AWS 考题极少用「这个功能不存在」当正确答案
  —— 出题人想考「你知不知道该怎么做」，不是「你知不知道做不到」

判断倾向：
  选项越【具体、像操作步骤】→ 越可能是答案
  选项越【笼统的否定判断】  → 越可能是干扰项
```

---

## 💡 Q7 答对值得记一笔（最狡猾的诱饵）

**场景：** EC2 上的应用需要读写 S3，最安全的凭证方式？
**D 选项诱饵：** 把 access key 存在 Parameter Store 里由应用读取

听起来比写在代码里安全，但**根本没解决问题**：
1. 应用还是得**先有凭证**才能读 Parameter Store → 先有鸡还是先有蛋
2. 长期 access key 本身就是风险（不会自动过期、泄露后一直有效）

### 🔑 IAM Role 的本质 = 「不需要凭证的凭证」

EC2 通过实例元数据服务自动拿到**临时的、自动轮换的**凭证，
**全程没有任何长期密钥落地。**

> **凡是「EC2 / Lambda / ECS 怎么访问别的 AWS 服务」→ 答案永远是 IAM Role，不要犹豫。**

---

# 监控审计四件套：9/10（90%）— 2026-09-13

答对：CloudTrail 查谁、CloudWatch Agent 内存指标、Config 配置历史、Data Events 默认不记录、
　　　Organization Trail、Trusted Advisor 需 Business、VPC Flow Logs、Metric Filter、反向题 Q10
答错：EC2 自动恢复

---

## 错题 15：EC2 自动恢复 —— ASG 替换 vs CloudWatch Recover

**场景：** EC2 因**底层硬件故障**时自动恢复，**且保持原有私有 IP 和实例 ID**

**我的答案：** A) ASG 自动替换实例　**正确答案：** B) CloudWatch Alarm + EC2 Recover 动作

### 错因 —— 漏掉后半句限定条件（与错题 12 同一模式）

| 方案 | 结果 |
|---|---|
| **ASG 自动替换** | 起一台**全新实例** → 实例 ID 变、私有 IP 变 ❌ |
| **CloudWatch Recover** | **同一台实例**迁到新硬件重启 → ID / 私有 IP / EIP **全部保留** ✅ |

> 如果题干**没有**「保持原有 IP 和 ID」那句，ASG 反而是更常见更好的答案。
> **是那句限定条件把答案改掉的。**

### 🔑 Recover 的触发条件（本身就是考点）

```
StatusCheckFailed_System   = AWS 侧的硬件/网络故障  → Recover 有用 ✅
StatusCheckFailed_Instance = 你自己的 OS 挂了       → Recover 没用 ❌
                                                      （换硬件重启还是同一个坏 OS）
```

---

## 🚨🚨 重要模式识别：「约束词决定答案」已栽第 N 次

| 错题 | 题干的两层 | 我抓住的 | 我漏掉的 |
|---|---|---|---|
| 错题 1（Aurora） | 快速恢复 + **不影响现有库** | 快速恢复 | **不影响现有库** |
| 错题 12（Kinesis） | 分散均匀 + **严格顺序** | 分散均匀 | **严格顺序** |
| 错题 15（EC2） | 故障自愈 + **保留 ID/IP** | 故障自愈 | **保留 ID/IP** |

**规律：抓住了「主需求」，漏掉了后半句的「限定条件」。**

AWS 出题人恰恰把答案藏在后半句里 —— **因为主需求往往有好几个方案都满足，
限定条件才是唯一的筛子。**

### ✅ 考场上的具体动作

> 读到题干里的 **「且」「同时」「并保持」「不影响」「最低成本」「不能」** 这类词时，
> **停一秒，把后面那句单独圈出来**，再逐个选项核对。

**这是我目前最顽固的失分点，考前最后一天必须重读本节。**

---

# 防护五件套：10/10（100%）— 2026-09-13 🎯 P4 收官

**无错题。** 出题前已用脚本校验选项行无加粗泄题 → 数据有效。

全部答对的考点：

| # | 考点 | 答案 |
|---|---|---|
| 1 | SQL 注入 / XSS 过滤 | **WAF** |
| 2 | 实例被入侵、恶意 IP 通信、挖矿 | **GuardDuty** |
| 3 | EC2 / ECR 镜像的 CVE 漏洞 | **Inspector** |
| 4 | S3 里的身份证号 / 信用卡号 | **Macie** |
| 5 | NLB 后面防 SQL 注入 | **NLB 前加 CloudFront + WAF**（两步推理） |
| 6 | DDoS + 专家支持 + 费用补偿 | **Shield Advanced** |
| 7 | 地理封禁 + 单 IP 限流 | **WAF（Geo Match + Rate-based Rule）** |
| 8 | 多账户集中查看所有安全发现 | **Security Hub** |
| 9 | Shield Standard 特性（正向题） | **免费且自动开启** |
| 10 | GuardDuty 特性（反向题） | 错误项 = **需先手动开 VPC Flow Logs** |

## 📌 Q5 值得记一笔：识破「对了一半」的选项

```
第一步：SQL 注入 → 需要 WAF        （关键词直取）✅ 很多人推到这里就停了
第二步：WAF 挂不上 NLB → 怎么办？   （能力边界）
        → 在前面加一个 WAF 能挂的东西 → CloudFront
```

A 选项「直接给 NLB 关联 WAF」之所以是陷阱，**正因为第一步的答案是对的**。

> 与**错题 13（Firehose）**同一模式：前半句判断正确，结论推错。
> 🔑 **遇到答案「看起来就是它」时，多问一句：这个服务在这个具体位置能用吗？**

## 📌 WAF 的五个挂载点（务必背死）

**CloudFront / ALB / API Gateway / AppSync / Cognito User Pool**

🚨 **挂不上 NLB**（NLB 是 L4，WAF 是 L7）

---

# P4 安全 + 监控 总成绩

| 模块 | 得分 |
|---|---|
| KMS + 密钥凭据管理 | 9/10（错题 14） |
| 监控审计四件套 | 9/10（错题 15） |
| 防护五件套 | 10/10 🎯 |
| **合计** | **28/30（93%）** |
